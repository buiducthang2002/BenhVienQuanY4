@{
    ViewData["Title"] = "Hủy Chuyển Phòng";
}

<div class="container mt-4">
    <h4 class="page-title text-center mb-4">🔄 HỦY CHUYỂN PHÒNG</h4>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-0">
                <div class="card-header">
                    <h5 class="mb-0">📝 Nhập thông tin</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Delete" method="post" id="deleteForm">
                        <div class="mb-3">
                            <label for="makcb" class="form-label fw-bold">Mã KCB:</label>
                            <input type="text"
                                   name="makcb"
                                   id="makcb"
                                   class="form-control"
                                   placeholder="Nhập Mã KCB cần hủy..."
                                   required
                                   autofocus />
                            <div class="form-text">Nhập mã KCB để hủy chuyển phòng và xóa dữ liệu liên quan</div>
                        </div>

                        <div class="alert alert-warning" role="alert">
                            <strong>⚠️ Cảnh báo:</strong> Thao tác này sẽ xóa vĩnh viễn dữ liệu và không thể khôi phục!
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <button type="submit"
                                    class="btn btn-danger btn-lg"
                                    onclick="return confirm('Bạn có chắc chắn muốn hủy chuyển phòng với Mã KCB này không?');">
                                🔄 Hủy chuyển phòng
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <div class="mt-4">
                <div class="info-box">
                    <h6 class="fw-bold mb-3">ℹ️ Thông tin chi tiết:</h6>
                    <ul class="mb-0">
                        <li>Xóa dữ liệu từ bảng <strong>chuyenphong</strong> với điều kiện: <code>makcb = '[Mã KCB]'</code></li>
                        <li>Xóa dữ liệu từ bảng <strong>khambenh</strong> với điều kiện: <code>makcb = '[Mã KCB]' AND nhokham = 1</code></li>
                        <li>Thao tác được thực hiện trong transaction để đảm bảo tính toàn vẹn dữ liệu</li>
                        <li>Nếu xảy ra lỗi, tất cả thay đổi sẽ được rollback tự động</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@* Success/Error messages *@
@if (TempData["Success"] != null)
{
    <div id="successToast" class="center-toast">
        @TempData["Success"]
    </div>
    <script>
        setTimeout(function () {
            var toast = document.getElementById('successToast');
            if (toast) {
                toast.style.transition = 'opacity 300ms ease';
                toast.style.opacity = '0';
                setTimeout(function () { toast.remove(); }, 350);
            }
        }, 3000);
    </script>
}

@if (TempData["Error"] != null)
{
    <div class="corner-toast toast-error" id="toastError">@TempData["Error"]</div>
    <script>
        setTimeout(function(){
            var t = document.getElementById('toastError');
            if(t){ t.style.transition='opacity 300ms'; t.style.opacity='0'; setTimeout(function(){ t.remove(); }, 350); }
        }, 3500);
    </script>
}

<style>
    body {
        background-color: #fff5f7;
        font-family: 'Segoe UI', sans-serif;
    }

    .page-title {
        color: #e91e63;
        font-weight: 700;
        letter-spacing: 0.5px;
    }

    .card {
        border-radius: 10px;
        overflow: hidden;
    }

    .card-header {
        background-color: #e91e63;
        color: white;
        border-bottom: none;
        padding: 15px 20px;
    }

    .card-body {
        padding: 25px;
    }

    .form-control {
        border: 2px solid #e91e63;
        border-radius: 5px;
        padding: 10px 15px;
        font-size: 16px;
    }

    .form-control:focus {
        border-color: #e91e63;
        box-shadow: 0 0 0 0.2rem rgba(233, 30, 99, 0.25);
    }

    .btn-danger {
        background-color: #e91e63 !important;
        border-color: #e91e63 !important;
        font-weight: 600;
        padding: 12px 30px;
    }

    .btn-danger:hover {
        background-color: #d81b60 !important;
        border-color: #d81b60 !important;
    }

    .info-box {
        background-color: #fff;
        border: 2px solid #e91e63;
        border-radius: 8px;
        padding: 20px;
    }

    .info-box ul {
        padding-left: 20px;
    }

    .info-box li {
        margin-bottom: 10px;
        line-height: 1.6;
    }

    .info-box code {
        background-color: #fff0f3;
        color: #e91e63;
        padding: 2px 6px;
        border-radius: 3px;
        font-family: 'Courier New', monospace;
        font-size: 14px;
    }

    .shadow-sm {
        box-shadow: 0 3px 10px rgba(233, 30, 99, 0.15);
    }

    .center-toast {
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: 1080;
        min-width: 320px;
        max-width: 80vw;
        background: rgba(33, 37, 41, 0.95);
        color: #fff;
        padding: 16px 20px;
        border-radius: 8px;
        box-shadow: 0 8px 24px rgba(0,0,0,0.2);
        text-align: center;
        font-weight: 600;
        font-size: 15px;
    }

    .corner-toast {
        position: fixed;
        top: 18px;
        right: 18px;
        z-index: 1080;
        min-width: 260px;
        max-width: 50vw;
        padding: 12px 16px;
        border-radius: 8px;
        box-shadow: 0 8px 24px rgba(0,0,0,0.15);
        font-weight: 500;
        font-size: 14px;
    }

    .toast-error {
        background: #dc3545;
        color: white;
    }
</style>
